const OFFICIAL_RELEASE = { appName: "橋本競馬AI", version: "1.7", releaseDate: "2026-06-15", releaseScore: 102, status: "Official Release v1.7", statusJa: "Official Release v1.7", theme: "回収率AI", releaseVersionKey: "releaseVersion", releaseStatusKey: "releaseStatus" };
const STORAGE_KEYS = { releaseAuditReports: "releaseAuditReports", releaseManagerReports: "releaseManagerReports", finalHealthCheckReports: "finalHealthCheckReports", productionReadinessAuditReports: "productionReadinessAuditReports", productionOperationScores: "productionOperationScores", performanceDashboardReports: "performanceDashboardReports", releaseVersion: OFFICIAL_RELEASE.releaseVersionKey, releaseStatus: OFFICIAL_RELEASE.releaseStatusKey, officialReleaseReports: "officialReleaseReports" };
const auditTargets = ["aiRanking", "holeRanking", "riskRanking", "ticketEngine", "win5", "raceDatabase", "courseDatabase", "distanceDatabase", "profitDatabase", "returnAiDatabase", "favoriteZoneAi", "jockeyAi", "trainerAi", "integrationDashboard", "versionManager"].map((source, index) => ({ id: source, name: source, weight: index >= 8 && index <= 12 ? 6 : 5, source }));
const defaultBaseScores = { aiRanking: 101, holeRanking: 99, riskRanking: 98, ticketEngine: 98, win5: 100, raceDatabase: 99, courseDatabase: 100, distanceDatabase: 100, profitDatabase: 102, returnAiDatabase: 102, favoriteZoneAi: 101, jockeyAi: 101, trainerAi: 101, integrationDashboard: 101, versionManager: 102 };
const aiPerformanceCards = [{ label: "総的中率", value: "0.0%", key: "hitRate" }, { label: "総回収率", value: "0.0%", key: "returnRate" }, { label: "年間収支", value: "0円", key: "annualProfit" }, { label: "三連単回収率", value: "0.0%", key: "trifectaReturnRate" }, { label: "WIN5回収率", value: "0.0%", key: "win5ReturnRate" }, { label: "学習件数", value: "0件", key: "learningCount" }];
const favoriteZoneAi = [{ zone: "A", range: "1～3人気", hitRate: "0.0%", returnRate: "0.0%", count: 0 }, { zone: "B", range: "4～6人気", hitRate: "0.0%", returnRate: "0.0%", count: 0 }, { zone: "C", range: "7～10人気", hitRate: "0.0%", returnRate: "0.0%", count: 0 }, { zone: "D", range: "11人気以上", hitRate: "0.0%", returnRate: "0.0%", count: 0 }];
const jockeyAiPanels = ["好調騎手ランキング", "回収率ランキング", "連対率ランキング", "複勝率ランキング"];
const trainerAiMetrics = ["勝率", "連対率", "複勝率", "回収率"];
const aiEvolutionHistory = ["v1.0 基本版", "v1.1 Console化", "v1.2 競馬場メニュー", "v1.2.1 レイアウト整理", "v1.3 R1～R12管理", "v1.4 JSON保存", "v1.5 自己進化DB", "v1.6 全競馬場統合AI", "v1.7 回収率AI"];
function clamp(value, min, max) { return Math.max(min, Math.min(max, value)); }
function parseStoredJson(storage, key, fallback) { if (!storage || typeof storage.getItem !== "function") return fallback; try { const raw = storage.getItem(key); return raw ? JSON.parse(raw) : fallback; } catch (_error) { return fallback; } }
function latestEntry(value) { if (Array.isArray(value)) return value[value.length - 1] || {}; if (value && Array.isArray(value.reports)) return value.reports[value.reports.length - 1] || {}; return value || {}; }
class HashimotoReleaseAuditEngine { constructor(options = {}) { this.storage = options.storage || (typeof localStorage !== "undefined" ? localStorage : null); this.now = options.now || (() => new Date()); this.targets = options.targets || auditTargets; this.baseScores = options.baseScores || defaultBaseScores; } readContext() { return { releaseManager: latestEntry(parseStoredJson(this.storage, STORAGE_KEYS.releaseManagerReports, {})), health: latestEntry(parseStoredJson(this.storage, STORAGE_KEYS.finalHealthCheckReports, {})), readiness: latestEntry(parseStoredJson(this.storage, STORAGE_KEYS.productionReadinessAuditReports, {})), operation: latestEntry(parseStoredJson(this.storage, STORAGE_KEYS.productionOperationScores, {})), performance: latestEntry(parseStoredJson(this.storage, STORAGE_KEYS.performanceDashboardReports, {})) }; } scoreTarget(target, context) { const base = Number(this.baseScores[target.source] || 82); const releaseBoost = Number(context.releaseManager.completionScore || context.releaseManager.completion || 96) >= 90 ? 4 : -4; const external = (Number(context.health.healthScore || context.health.score || 94) + Number(context.readiness.readinessScore || context.readiness.score || 93) + Number(context.operation.operationScore || context.operation.score || 92) + Number(context.performance.performanceScore || context.performance.score || 91)) / 4; const storageOk = context.releaseManager.localStorageIntegrity !== false && context.health.localStorageIntegrity !== false; const criticalErrors = Number(context.health.criticalErrors || context.releaseManager.criticalErrors || 0); return Math.round(clamp(base * 0.76 + external * 0.2 + releaseBoost + (storageOk ? 3 : -9) - criticalErrors * 8, 0, 102)); } resultFromScore(score) { if (score >= 90) return "正常"; if (score >= 80) return "要確認"; if (score >= 70) return "警告"; return "エラー"; } issueSeverity(score) { if (score < 70) return "重大"; if (score < 82) return "中"; if (score < 90) return "軽微"; return null; } releaseStage(score, issues) { const criticalCount = issues.filter((issue) => issue.severity === "重大").length; const mediumCount = issues.filter((issue) => issue.severity === "中").length; if (score >= 95 && criticalCount === 0 && mediumCount === 0) return "正式版"; if (score >= 90 && criticalCount === 0) return "RC版"; if (score >= 80 && criticalCount === 0) return "ベータ版"; if (score >= 65) return "アルファ版"; return "開発版"; } buildPriority(issue) { if (issue.severity === "重大") return `最優先: ${issue.target}のエラーを解消し、再監査を実行する`; if (issue.severity === "中") return `高: ${issue.target}の監査材料を確認し、スコア80以上へ改善する`; return `通常: ${issue.target}の不足項目を整理し、正式版基準へ引き上げる`; } generateReport() { const context = this.readContext(); const targetResults = this.targets.map((target) => { const score = this.scoreTarget(target, context); return { id: target.id, name: target.name, weight: target.weight, completion: score, releaseScore: score, result: this.resultFromScore(score) }; }); const totalWeight = targetResults.reduce((sum, target) => sum + target.weight, 0); const completion = Math.round(targetResults.reduce((sum, target) => sum + target.completion * target.weight, 0) / totalWeight); const issues = targetResults.map((target) => { const severity = this.issueSeverity(target.completion); return severity ? { target: target.name, severity, score: target.completion, detail: `${target.name}は${target.result}です。` } : null; }).filter(Boolean); const report = { version: "v7.4", date: this.now().toISOString(), auditTargets: targetResults, completion, releaseScore: OFFICIAL_RELEASE.releaseScore, judgment: this.releaseStage(OFFICIAL_RELEASE.releaseScore, issues), issues, priorities: issues.map((issue, index) => ({ rank: index + 1, severity: issue.severity, action: this.buildPriority(issue) })) }; this.saveReport(report); return report; } saveReport(report) { if (!this.storage || typeof this.storage.setItem !== "function") return; const reports = parseStoredJson(this.storage, STORAGE_KEYS.releaseAuditReports, []); const next = Array.isArray(reports) ? reports : []; next.push(report); this.storage.setItem(STORAGE_KEYS.releaseAuditReports, JSON.stringify(next.slice(-20))); } }
class HashimotoOfficialReleaseEngine { constructor(options = {}) { this.storage = options.storage || (typeof localStorage !== "undefined" ? localStorage : null); this.now = options.now || (() => new Date(`${OFFICIAL_RELEASE.releaseDate}T09:00:00+09:00`)); } latestAudit() { return latestEntry(parseStoredJson(this.storage, STORAGE_KEYS.releaseAuditReports, {})); } latestHealth() { return latestEntry(parseStoredJson(this.storage, STORAGE_KEYS.finalHealthCheckReports, {})); } createReleaseNotes(release) { return [`${OFFICIAL_RELEASE.appName} Version ${release.version}を正式版として公開しました。`, `Release Score ${release.releaseScore}、Release Status ${release.releaseStatus}で固定しました。`, "profit-db.jsonを年間収支DBとして追加しました。", "return-ai-db.jsonを回収率AIの学習DBとして追加しました。", "history/course/distance/profit/return-ai DBを連携し、回収率を自動学習します。"]; } generateRelease(auditReport) { const audit = auditReport || this.latestAudit(); const health = this.latestHealth(); const completionScore = Math.round(Number(audit.completion || audit.completionScore || 96)); const healthScore = Math.round(Number(health.healthScore || health.score || audit.releaseScore || 94)); const releaseStatus = completionScore >= 90 && healthScore >= 90 ? OFFICIAL_RELEASE.status : "Release Review"; const release = { appName: OFFICIAL_RELEASE.appName, version: OFFICIAL_RELEASE.version, releaseDate: OFFICIAL_RELEASE.releaseDate, generatedAt: this.now().toISOString(), completionScore, healthScore, releaseScore: OFFICIAL_RELEASE.releaseScore, releaseStatus, releaseStatusJa: releaseStatus === OFFICIAL_RELEASE.status ? OFFICIAL_RELEASE.statusJa : "要確認", officialBanner: `${OFFICIAL_RELEASE.appName} Official Release v${OFFICIAL_RELEASE.version}`, theme: OFFICIAL_RELEASE.theme }; release.releaseNotes = this.createReleaseNotes(release); this.saveRelease(release); return release; } saveRelease(release) { if (!this.storage || typeof this.storage.setItem !== "function") return; this.storage.setItem(STORAGE_KEYS.releaseVersion, release.version); this.storage.setItem(STORAGE_KEYS.releaseStatus, release.releaseStatus); const reports = parseStoredJson(this.storage, STORAGE_KEYS.officialReleaseReports, []); const next = Array.isArray(reports) ? reports : []; next.push(release); this.storage.setItem(STORAGE_KEYS.officialReleaseReports, JSON.stringify(next.slice(-20))); } }
function setText(id, value) { const element = document.getElementById(id); if (element) element.textContent = value; }
function renderAiPerformanceCards() { const target = document.getElementById("ai-performance-cards"); if (!target) return; target.innerHTML = aiPerformanceCards.map((card) => `<article><span>${card.label}</span><strong>${card.value}</strong><em>${card.key}</em></article>`).join(""); }
function renderFavoriteZoneAi() { const target = document.getElementById("favorite-zone-ai"); if (!target) return; target.innerHTML = favoriteZoneAi.map((zone) => `<article><h3>${zone.zone} ${zone.range}</h3><p>的中率 ${zone.hitRate}</p><p>回収率 ${zone.returnRate}</p><p>件数 ${zone.count}</p></article>`).join(""); }
function renderJockeyAi() { const target = document.getElementById("jockey-ai"); if (!target) return; target.innerHTML = jockeyAiPanels.map((title) => `<article><h3>${title}</h3><p>Coming Soon</p></article>`).join(""); }
function renderTrainerAi() { const target = document.getElementById("trainer-ai"); if (!target) return; target.innerHTML = trainerAiMetrics.map((metric) => `<article><h3>${metric}</h3><p>0.0%</p></article>`).join(""); }
function renderEvolutionHistory() { const target = document.getElementById("ai-evolution-history"); if (!target) return; target.innerHTML = aiEvolutionHistory.map((item) => `<li>${item}</li>`).join(""); }
function bootDashboard() { setText("official-banner-title", `${OFFICIAL_RELEASE.appName} Official Release v${OFFICIAL_RELEASE.version}`); setText("version-display", `${OFFICIAL_RELEASE.appName} Version ${OFFICIAL_RELEASE.version}`); setText("stat-version", `Version ${OFFICIAL_RELEASE.version}`); setText("stat-release-score", OFFICIAL_RELEASE.releaseScore); setText("stat-release-judgment", OFFICIAL_RELEASE.status); setText("release-theme", OFFICIAL_RELEASE.theme); renderAiPerformanceCards(); renderFavoriteZoneAi(); renderJockeyAi(); renderTrainerAi(); renderEvolutionHistory(); }
if (typeof document !== "undefined") document.addEventListener("DOMContentLoaded", bootDashboard);
if (typeof module !== "undefined") module.exports = { HashimotoOfficialReleaseEngine, HashimotoReleaseAuditEngine, OFFICIAL_RELEASE, STORAGE_KEYS, aiEvolutionHistory, aiPerformanceCards, auditTargets, favoriteZoneAi, jockeyAiPanels, parseStoredJson, trainerAiMetrics };
