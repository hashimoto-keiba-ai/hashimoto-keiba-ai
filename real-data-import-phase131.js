(function () {
  const importTargets = ["Tokyo", "Nakayama", "Hanshin", "Kyoto", "Chukyo", "Fukushima", "Niigata", "Kokura", "Hakodate", "Sapporo", "WIN5", "OS Updates", "Prediction Logs", "Result Logs"];
  const supportedFormats = ["CSV", "JSON", "Markdown text"];
  const databaseMap = { "Prediction Logs": "Prediction Database", "Result Logs": "Result Verification Database", "OS Updates": "OS Update Database", WIN5: "WIN5 Database", Trifecta: "Trifecta Database", ROI: "ROI Database", AI: "AI Index Database" };
  const fallbackDatabase = { databaseName: "realDataImportDatabase", phase: "Phase13-1", records: [{ importDate: "2026-06-06", dataType: "Prediction Logs", racecourse: "Tokyo", sourceName: "Tokyo sample prediction CSV", recordCount: 2, importStatus: "Preview Ready", importedRecords: [{ racecourse: "Tokyo", raceName: "Tokyo 11R Future Simulation Stakes", horseNumber: 1, horseName: "AI Commander", aiScore: 91, popularity: 1 }, { racecourse: "Tokyo", raceName: "Tokyo 11R Future Simulation Stakes", horseNumber: 3, horseName: "Kami Flash", aiScore: 84, popularity: 6 }], errorCount: 0, notes: "Seed import record for Phase13-1 parser, preview, and validation flow." }] };
  function toCamelCase(value) { return String(value || "").trim().replace(/^\uFEFF/, "").replace(/[^a-zA-Z0-9]+(.)/g, (_, char) => char.toUpperCase()).replace(/^[A-Z]/, (char) => char.toLowerCase()); }
  function normalizeValue(value) { const trimmed = String(value ?? "").trim(); if (/^-?\d+(\.\d+)?$/.test(trimmed)) return Number(trimmed); return trimmed; }
  function normalizeRecord(record) { return Object.entries(record || {}).reduce((normalized, [key, value]) => { normalized[toCamelCase(key)] = normalizeValue(value); return normalized; }, {}); }
  function parseCsv(text) { const rows = String(text || "").trim().split(/\r?\n/).filter(Boolean); if (!rows.length) return []; const headers = rows[0].split(",").map(toCamelCase); return rows.slice(1).map((row) => headers.reduce((record, header, index) => { record[header] = normalizeValue(row.split(",")[index]); return record; }, {})); }
  function parseJson(text) { const parsed = JSON.parse(text); const rows = Array.isArray(parsed) ? parsed : parsed.records || parsed.importedRecords || []; return rows.map((record) => normalizeRecord(record)); }
  function parseMarkdown(text) { return String(text || "").split(/\r?\n/).map((line) => line.trim()).filter((line) => line && !line.startsWith("#")).map((line) => { const record = {}; line.split(/[|,]/).forEach((part) => { const [rawKey, ...rawValue] = part.split(":"); if (!rawKey || !rawValue.length) return; record[toCamelCase(rawKey)] = normalizeValue(rawValue.join(":")); }); return normalizeRecord(record); }).filter((record) => Object.keys(record).length > 0); }
  function parseImport(text, format) { if (format === "CSV") return parseCsv(text).map(normalizeRecord); if (format === "JSON") return parseJson(text); if (format === "Markdown text") return parseMarkdown(text); throw new Error(`Unsupported import format: ${format}`); }
  function validateRecord(record) { const errors = []; if (!record.racecourse) errors.push("Missing racecourse"); if (!record.raceName && !record.race) errors.push("Missing race name"); if (record.horseNumber === undefined || record.horseNumber === "") errors.push("Missing horse number"); if (!record.horseName) errors.push("Missing horse name"); if (record.aiScore !== undefined && (Number(record.aiScore) < 0 || Number(record.aiScore) > 100 || Number.isNaN(Number(record.aiScore)))) errors.push("Invalid AI score"); if (record.popularity !== undefined && (Number(record.popularity) < 1 || Number.isNaN(Number(record.popularity)))) errors.push("Invalid popularity"); return errors; }
  function mapToDatabase(dataType, record) { const targetDatabase = databaseMap[dataType] || databaseMap[record.dataType] || "AI Index Database"; return { targetDatabase, record }; }
  function previewImport({ text, format, dataType, racecourse, sourceName }) { const parsedRecords = parseImport(text, format); const validation = parsedRecords.map((record, index) => ({ index, record, errors: validateRecord(record), mapping: mapToDatabase(dataType, record).targetDatabase })); const errorCount = validation.reduce((total, item) => total + item.errors.length, 0); return { importDate: new Date().toISOString().slice(0, 10), dataType, racecourse, sourceName, recordCount: parsedRecords.length, importStatus: errorCount ? "Validation Error" : "Preview Ready", importedRecords: parsedRecords, errorCount, validation, notes: errorCount ? "Fix validation errors before import." : "Preview passed validation." }; }
  function createImportReport(preview) { const successfulRecords = preview.validation.filter((item) => item.errors.length === 0); return { ...preview, importStatus: preview.errorCount ? "Partial Import Blocked" : "Import Ready", successCount: successfulRecords.length, targetDatabases: [...new Set(preview.validation.map((item) => item.mapping))], latestImports: preview.importedRecords.slice(0, 5) }; }
  function buildDashboard(database) { const source = database || fallbackDatabase; const records = source.records || []; const successCount = records.filter((record) => Number(record.errorCount) === 0).length; const errorCount = records.reduce((total, record) => total + (Number(record.errorCount) || 0), 0); return { databaseName: source.databaseName || "realDataImportDatabase", phase: source.phase || "Phase13-1", importTargets, supportedFormats, databaseMap, records, widget: { latestImports: records.slice(0, 5), importSuccessCount: successCount, importErrorCount: errorCount } }; }
  function setText(id, value) { const element = document.getElementById(id); if (element) element.textContent = value; }
  function renderCards(id, items, renderer) { const element = document.getElementById(id); if (!element) return; element.innerHTML = items.map(renderer).join(""); }
  function renderTable(records) { const table = document.getElementById("phase131-import-database"); if (!table) return; table.innerHTML = records.map((record) => `<tr><td>${record.importDate}</td><td>${record.dataType}</td><td>${record.racecourse}</td><td>${record.sourceName}</td><td>${record.recordCount}</td><td>${record.importStatus}</td><td>${record.importedRecords.length}</td><td>${record.errorCount}</td><td>${record.notes}</td></tr>`).join(""); }
  function renderDashboard(report) { setText("phase131-widget-success", `${report.widget.importSuccessCount}`); setText("phase131-widget-errors", `${report.widget.importErrorCount}`); setText("phase131-widget-latest", report.widget.latestImports[0] ? report.widget.latestImports[0].sourceName : "--"); renderCards("phase131-import-targets", report.importTargets, (target) => `<article><span>${target}</span><strong>Target</strong><em>import ready</em></article>`); renderCards("phase131-import-formats", report.supportedFormats, (format) => `<article><span>${format}</span><strong>Supported</strong><em>parser enabled</em></article>`); renderCards("phase131-latest-imports", report.widget.latestImports, (record) => `<article><span>${record.sourceName}</span><strong>${record.importStatus}</strong><em>${record.recordCount} records / ${record.errorCount} errors</em></article>`); renderTable(report.records); }
  async function loadDatabase() { if (typeof fetch !== "function") return fallbackDatabase; try { const response = await fetch("data/realDataImportDatabase.json", { cache: "no-store" }); if (!response.ok) throw new Error("realDataImportDatabase fetch failed"); return await response.json(); } catch (error) { return fallbackDatabase; } }
  async function bootstrap() { const database = await loadDatabase(); const report = buildDashboard(database); renderDashboard(report); window.HashimotoPhase131RealDataImportReport = report; }
  window.HashimotoPhase131RealDataImport = { buildDashboard, createImportReport, fallbackDatabase, importTargets, mapToDatabase, parseImport, previewImport, supportedFormats, validateRecord };
  if (typeof document !== "undefined") document.addEventListener("DOMContentLoaded", bootstrap);
})();
