"use strict";const assert=require("assert"),fs=require("fs"),path=require("path"),core=require("../phase26-37-phase26-integrity-audit-check-core.js");
const clock={now:()=>new Date("2026-08-06T09:00:00Z")},op=reason=>({performedBy:"human-auditor",reason,explicitConfirmation:true});
const closure=(o={})=>({postCorrectionClosureExecutionRecordId:"closure-36",registrationRecordId:"registration-1",sourceRecordId:"source-1",raceId:"race-1",targetRecordType:"race",targetRecordKey:"race-1",closureExecutionStatus:"ready_for_phase26_integrity_audit",closureExecutionResult:"completed",closureCompleted:true,closurePartiallyCompleted:false,closureFailed:false,closureCancelled:false,closureStopped:false,closureRolledBack:false,rollbackRequired:false,postExecutionVerificationRequired:false,additionalEvidenceRequired:false,additionalReviewRequired:false,followUpRequired:false,outstandingItems:[],abnormalityDetected:false,criticalIssueDetected:false,unresolvedIssueCount:0,closureApprovalRecordValid:true,closureApprovalStatusValid:true,closureApprovalResultValid:true,approvalWithinValidityPeriod:true,closureCheckRecordValid:true,managementRecordValid:true,acceptanceRecordValid:true,decisionRecordValid:true,verificationRecordValid:true,correctionExecutionRecordValid:true,targetIdentityConfirmed:true,targetKeyConfirmed:true,registrationReferenceConfirmed:true,hashResultConfirmed:true,versionResultConfirmed:true,approvedScopeConfirmed:true,executedScopeConfirmed:true,scopeDeviationAbsent:true,checklistConfirmed:true,operatorConfirmed:true,executionEvidenceConfirmed:true,auditTrailConfirmed:true,rollbackStatusConfirmed:true,abnormalitiesResolved:true,criticalIssuesAbsent:true,warningsReviewed:true,safetyFlagsConfirmed:true,protectedMode:true,planOnly:true,privateLocalOnly:true,automaticClosureAllowed:false,automaticApplicationAllowed:false,automaticLearningAllowed:false,publicReleaseAllowed:false,externalTransmissionAllowed:false,registeredDataMutated:false,appliedToPrediction:false,appliedToLearning:false,sentExternally:false,auditTrail:[{event:"ready"}],...o});
const records=Array.from({length:36},(_,i)=>({phaseNumber:i+1,phaseId:`Phase26-${i+1}`,recordId:`record-${i+1}`,targetRecordKey:"race-1",raceId:"race-1",protectedMode:true,planOnly:true,privateLocalOnly:true,publicReleaseAllowed:false,externalTransmissionAllowed:false,auditTrail:[{event:"recorded"}]}));
const input={auditedBy:"Auditor",recordedBy:"Recorder",auditEnvironment:"Private Local",auditDevice:"local-pc",auditLocation:"local",auditMethod:"manual checklist",auditReference:"audit-sheet"};
assert(core.isEligibleClosure(closure()));assert.strictEqual(core.extractAuditCandidates([closure()],[]).length,1);assert(core.validateAuditStartGate(closure(),records,[]).eligible);assert(!core.validateAuditStartGate(closure(),records.slice(1),[]).eligible);assert(!core.validateAuditStartGate(closure(),records,[{postCorrectionClosureExecutionRecordId:"closure-36"}]).eligible);
core.resetRegistry();let x=core.createIntegrityAuditRecord(closure(),records,input,op("create audit"),clock);assert(x.created,x.reasons);let r=x.record;x=core.startManualAudit(r,{manualStartConfirmation:true},op("start"),clock);assert(x.transitioned);x=core.recordAuditProgress(x.record,{auditedRecordIds:records.map(v=>v.recordId)},op("progress"),clock);assert(x.transitioned);const checks=core.runIntegrityChecks(records);assert(checks.passed);x=core.completeAudit(x.record,{auditResult:"pass",auditReason:"all checks passed",auditSummary:"integrity confirmed",nextAction:"phase26 completion decision",records,checks},op("complete"),clock);assert(x.completed,x.reasons);assert.strictEqual(x.record.auditStatus,"ready_for_phase26_completion_decision");assert(x.record.integrityConfirmed);assert(!core.transition(x.record,"manual_integrity_audit_in_progress",op("overwrite"),clock).transitioned);assert(core.createCompletionDecisionCandidate(x.record,op("handoff")).created);
let bad=records.slice(1);assert(!core.checkPhaseContinuity(bad).valid);assert(!core.checkTargetConsistency([...records,{targetRecordKey:"other"}]).valid);assert(!core.checkHashes([{recordId:"x",hashRequired:true}]).valid);assert(!core.checkVersions([{recordId:"x",versionRequired:true}]).valid);assert(!core.checkEvidence([{recordId:"x",evidenceRequired:true}]).valid);assert(!core.checkDuplicatesMissingOrphans([{recordId:"x"},{recordId:"x"}]).valid);assert(!core.checkSafetyFlags([{recordId:"x",automaticCorrectionAllowed:true}]).valid);
x=core.createIntegrityAuditRecord(closure({targetRecordKey:"race-2"}),records,input,op("bad target"),clock);assert(!x.created);x=core.createIntegrityAuditRecord(closure(),records,input,op("create failure"),clock);r=x.record;r=core.startManualAudit(r,{manualStartConfirmation:true},op("start failure"),clock).record;r=core.recordAuditProgress(r,{},op("progress failure"),clock).record;x=core.completeAudit(r,{auditResult:"fail",auditReason:"missing phase",nextAction:"manual investigation",records:bad},op("fail"),clock);assert(x.completed&&x.record.auditStatus==="integrity_failed");assert(!core.createCompletionDecisionCandidate(x.record,op("blocked handoff")).created);
const memory={value:null,setItem(k,v){this.value=v},getItem(){return this.value}};assert(core.saveRecords(memory,[r]).saved);assert(core.loadRecords(memory).loaded);memory.value=JSON.stringify({schemaVersion:"old",records:[]});assert(!core.loadRecords(memory).loaded);
const root=path.join(__dirname,".."),html=fs.readFileSync(path.join(root,"private-local.html"),"utf8"),css=fs.readFileSync(path.join(root,"phase26-37.css"),"utf8"),readme=fs.readFileSync(path.join(root,"README.md"),"utf8"),source=fs.readFileSync(path.join(root,"phase26-37-phase26-integrity-audit-check-core.js"),"utf8");for(const text of["phase26-37-phase26-integrity-audit-check","Phase26全体整合性・監査確認コア","監査対象クローズ実行記録","欠損記録","重複記録","孤立記録","不正参照","hash結果","version結果","監査証跡","次工程移行可否","Private Local only","PLAN_ONLY","protectedMode","自動監査完了なし","Public公開なし","外部送信なし"])assert(html.includes(text),text);assert(css.includes(".phase2637-panel")&&readme.includes("Phase26-37 Phase26 Integrity Audit Check Core"));for(const p of[/fetch\s*\(/,/XMLHttpRequest/,/WebSocket\s*\(/,/automaticAuditCompletionAllowed:\s*true/,/registeredDataMutated:\s*true/])assert(!p.test(source));console.log("phase26IntegrityAuditCheckCore.test.js: PASS");
